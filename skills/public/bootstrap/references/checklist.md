# Harness Bootstrap Checklist

- Plugin exists at `usr/plugins/agent_harness`
- `plugin.yaml` is present
- `Install.md` is present
- `scripts/check_deerflow_harness.py` is present
- `scripts/import_deerflow_public_skills.py` is present
- `skills/public/bootstrap/SKILL.md` is present
- `skills/public/find-skills/SKILL.md` is present
- DeerFlow-compatible modes are `flash`, `standard`, `pro`, `ultra`
- Optional DeerFlow source can be validated or imported
